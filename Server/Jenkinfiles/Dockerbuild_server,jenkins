#!groovy
properties([disableConcurrentBuilds()])

pipeline {
    agent any
    options {
         timestamps()
    }
    stages {
        stage("create docker image") {
            steps {
                dir ('Server') {
                    sh 'docker build server .'
                }
            }
        }
    }
}