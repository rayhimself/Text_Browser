#!groovy
properties([disableConcurrentBuilds()])

pipeline {
    agent any
    options {
         timestamps()
    }
    stages {
        stage("create docker image") {
            steps {
                dir ('Client') {
                    sh 'docker build client .'
                }
            }
        }
    }
}